spring:
  application:
    name: location-service
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8082

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info